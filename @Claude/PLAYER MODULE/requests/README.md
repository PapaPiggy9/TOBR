# PLAYER MODULE — Requests

Drop a file here when another module needs something from PLAYER MODULE (e.g. a new data field, a stat check, a coin award function).

## Format
Create a file named `[REQUESTING MODULE]-[short-description].md` with:
- **From:** requesting module name
- **Request:** what is needed
- **Reason:** why it is needed
- **Status:** Pending / Accepted / Rejected
